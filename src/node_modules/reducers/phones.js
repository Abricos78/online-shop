import * as R from 'ramda'
import {
    FETCH_PHONES_SUCCESS
} from 'actionTypes'

let initialState = {}

export let phones = (state = initialState, {type , payload}) => {
    switch (type) {
        case FETCH_PHONES_SUCCESS:
            const newValues = R.indexBy(R.prop('id'), payload)
            return R.mergeAll(newValues, state)
        default:
            return state
    }
}